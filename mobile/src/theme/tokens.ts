import { colors } from './colors';
import { radius } from './radius';
import { spacing } from './spacing';
import { typography } from './typography';

const type = {
  xs: typography.label,
  sm: typography.bodySmall,
  md: typography.bodyLarge,
  lg: typography.titleSmall,
  xl: typography.title,
  xxl: typography.titleLarge,
  xxxl: 30,
};

export { colors, radius, spacing, type, typography };
